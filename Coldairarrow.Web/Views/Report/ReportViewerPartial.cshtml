@using LH.StoReports;
@using DevExpress.Web.Mvc;
@using DevExpress.Web.Mvc.UI;

66666666

@*@Html.DevExpress().ReportViewer(
    settings => {
        settings.Name = "ReportViewer1";
        settings.Report = Model.Report;
        settings.CallbackRouteValues = new { Controller = "Report", Action = "ReportViewerPartial", ReportID = Model.ReportID };
        settings.ExportRouteValues = new { Controller = "Report", Action = "ReportViewerExportTo", ReportID = Model.ReportID };
    }
).GetHtml()*@
